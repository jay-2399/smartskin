# Base de données (Render Postgres EU)
DATABASE_URL="postgresql://user:password@host:5432/smartskin"

# Auth.js
AUTH_SECRET="genere-avec: openssl rand -base64 32"

# Google OAuth (connexion un clic)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Resend (liens magiques par email)
RESEND_API_KEY=""

# ── Gemini ──
# Test / dev : clé Google AI Studio (https://aistudio.google.com/apikey).
# Sans cette clé, /api/analyze renvoie un bilan d'exemple (mode démo).
GEMINI_API_KEY=""

# Production UE (RGPD) : bascule vers Vertex AI europe-west (cf. gemini.ts).
GOOGLE_CLOUD_PROJECT=""
GOOGLE_CLOUD_LOCATION="europe-west1"
GOOGLE_APPLICATION_CREDENTIALS="./gcloud-key.json"
